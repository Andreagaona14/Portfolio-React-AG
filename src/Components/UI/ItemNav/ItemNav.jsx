/*import React from "react";
import { NavLink } from "react-router-dom";

export const ItemNav = ({ Word, link = '', classItem}) => {
  const to = link ? `/${link}` : '/';

  return(
    <li>
      <NavLink className={classItem} to={to} > {Word}</NavLink>
    </li>
  )
}
  */